package entity_test

import (
	"testing"
)

func Test_CreateCredentialsWithSuccess(t *testing.T) {
	// Create a valid credentials struct
	// NewCredentials("user", "pass", "addr", 1, "share", []WatcherMapping{})
}
